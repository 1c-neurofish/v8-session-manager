# Контейнерный конфиг v8-session-manager.

workPath: /var/lib/v8-session-manager/work

mcp:
  session_manager:
    bind_address: "0.0.0.0:4000"
    path: "/sessions"
  http:
    bind_address: "0.0.0.0:4001"
    path: "/mcp"
